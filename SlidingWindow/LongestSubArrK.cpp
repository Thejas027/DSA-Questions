// brute force approach
class BruteForce
{
public:
      int longestSubArr(vector<int> &num, int k)
      {
            int n = num.size();
            int maxlen = 0;
            for (int i = 0; i < n; i++)
            {
                  int sum = 0;
                  for (int j = i; j < n; j++)
                  {
                        sum += num[j];
                        if(sum )
                  }
            }
      }
}